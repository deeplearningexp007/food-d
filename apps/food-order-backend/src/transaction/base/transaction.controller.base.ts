/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TransactionService } from "../transaction.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TransactionCreateInput } from "./TransactionCreateInput";
import { Transaction } from "./Transaction";
import { TransactionFindManyArgs } from "./TransactionFindManyArgs";
import { TransactionWhereUniqueInput } from "./TransactionWhereUniqueInput";
import { TransactionUpdateInput } from "./TransactionUpdateInput";
import { TransactionStatusFindManyArgs } from "../../transactionStatus/base/TransactionStatusFindManyArgs";
import { TransactionStatus } from "../../transactionStatus/base/TransactionStatus";
import { TransactionStatusWhereUniqueInput } from "../../transactionStatus/base/TransactionStatusWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TransactionControllerBase {
  constructor(
    protected readonly service: TransactionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Transaction })
  @nestAccessControl.UseRoles({
    resource: "Transaction",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createTransaction(
    @common.Body() data: TransactionCreateInput
  ): Promise<Transaction> {
    return await this.service.createTransaction({
      data: {
        ...data,

        orders: data.orders
          ? {
              connect: data.orders,
            }
          : undefined,

        userModels: data.userModels
          ? {
              connect: data.userModels,
            }
          : undefined,
      },
      select: {
        amount: true,
        createdAt: true,
        id: true,

        orders: {
          select: {
            id: true,
          },
        },

        status: true,
        transactionType: true,
        updatedAt: true,

        userModels: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Transaction] })
  @ApiNestedQuery(TransactionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Transaction",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async transactions(@common.Req() request: Request): Promise<Transaction[]> {
    const args = plainToClass(TransactionFindManyArgs, request.query);
    return this.service.transactions({
      ...args,
      select: {
        amount: true,
        createdAt: true,
        id: true,

        orders: {
          select: {
            id: true,
          },
        },

        status: true,
        transactionType: true,
        updatedAt: true,

        userModels: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Transaction })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Transaction",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async transaction(
    @common.Param() params: TransactionWhereUniqueInput
  ): Promise<Transaction | null> {
    const result = await this.service.transaction({
      where: params,
      select: {
        amount: true,
        createdAt: true,
        id: true,

        orders: {
          select: {
            id: true,
          },
        },

        status: true,
        transactionType: true,
        updatedAt: true,

        userModels: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Transaction })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Transaction",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateTransaction(
    @common.Param() params: TransactionWhereUniqueInput,
    @common.Body() data: TransactionUpdateInput
  ): Promise<Transaction | null> {
    try {
      return await this.service.updateTransaction({
        where: params,
        data: {
          ...data,

          orders: data.orders
            ? {
                connect: data.orders,
              }
            : undefined,

          userModels: data.userModels
            ? {
                connect: data.userModels,
              }
            : undefined,
        },
        select: {
          amount: true,
          createdAt: true,
          id: true,

          orders: {
            select: {
              id: true,
            },
          },

          status: true,
          transactionType: true,
          updatedAt: true,

          userModels: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Transaction })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Transaction",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteTransaction(
    @common.Param() params: TransactionWhereUniqueInput
  ): Promise<Transaction | null> {
    try {
      return await this.service.deleteTransaction({
        where: params,
        select: {
          amount: true,
          createdAt: true,
          id: true,

          orders: {
            select: {
              id: true,
            },
          },

          status: true,
          transactionType: true,
          updatedAt: true,

          userModels: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/transactionStatuses")
  @ApiNestedQuery(TransactionStatusFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TransactionStatus",
    action: "read",
    possession: "any",
  })
  async findTransactionStatuses(
    @common.Req() request: Request,
    @common.Param() params: TransactionWhereUniqueInput
  ): Promise<TransactionStatus[]> {
    const query = plainToClass(TransactionStatusFindManyArgs, request.query);
    const results = await this.service.findTransactionStatuses(params.id, {
      ...query,
      select: {
        id: true,
        status: true,
        statusDate: true,

        transactions: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/transactionStatuses")
  @nestAccessControl.UseRoles({
    resource: "Transaction",
    action: "update",
    possession: "any",
  })
  async connectTransactionStatuses(
    @common.Param() params: TransactionWhereUniqueInput,
    @common.Body() body: TransactionStatusWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      transactionStatuses: {
        connect: body,
      },
    };
    await this.service.updateTransaction({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/transactionStatuses")
  @nestAccessControl.UseRoles({
    resource: "Transaction",
    action: "update",
    possession: "any",
  })
  async updateTransactionStatuses(
    @common.Param() params: TransactionWhereUniqueInput,
    @common.Body() body: TransactionStatusWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      transactionStatuses: {
        set: body,
      },
    };
    await this.service.updateTransaction({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/transactionStatuses")
  @nestAccessControl.UseRoles({
    resource: "Transaction",
    action: "update",
    possession: "any",
  })
  async disconnectTransactionStatuses(
    @common.Param() params: TransactionWhereUniqueInput,
    @common.Body() body: TransactionStatusWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      transactionStatuses: {
        disconnect: body,
      },
    };
    await this.service.updateTransaction({
      where: params,
      data,
      select: { id: true },
    });
  }
}
